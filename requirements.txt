# Core deep learning stack
torch>=2.1,<2.5
transformers>=4.45,<4.50
peft>=0.11,<0.14
accelerate>=0.30,<1.0
bitsandbytes>=0.43,<0.45      # 4-bit QLoRA
datasets>=2.20,<3.0

# Optional acceleration (install separately if your CUDA build supports it)
# flash-attn>=2.5    # Stage 1 uses Flash Attention 2 when available

# Data, metrics, utilities
numpy>=1.24,<2.1
pandas>=2.0,<2.3
scikit-learn>=1.3,<1.6
scipy>=1.10,<1.14
ftfy>=6.1                      # Encoding fix for the KTAS lookup JSON
PyYAML>=6.0
tqdm>=4.66

# Plotting (figure reproduction)
matplotlib>=3.7,<3.10
seaborn>=0.13,<0.14

# Baselines
openai>=1.30,<2.0              # Required only for the GPT-4o baseline
